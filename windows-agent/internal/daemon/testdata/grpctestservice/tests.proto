syntax = "proto3";

option go_package = "github.com/canonical/ubuntu-pro-for-wsl/windows-agent/internal/daemon/testdata/grpctestservice";

package grpctestservice;

service TestService {
    rpc Blocking (Empty) returns (Empty) {}
}

message Empty {
}
