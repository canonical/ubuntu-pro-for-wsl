syntax = "proto3";

option go_package = "github.com/canonical/ubuntu-pro-for-windows/wslserviceapi";

package wslserviceapi;

service WSL {
    rpc ApplyProToken (ProAttachInfo) returns (Empty) {}
    rpc Ping(Empty) returns (Empty) {}
}

message ProAttachInfo {
    string token = 1;
}

message Empty {}
