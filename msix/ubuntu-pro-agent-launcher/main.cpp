/// A Windows Application that creates an invisible pseudo-console to host the ubuntu-pro-agent.exe;
#include <windows.h>

int WINAPI wWinMain(HINSTANCE hInstance, HINSTANCE hPrevInstance,
                    PWSTR pCmdLine, int nCmdShow)
{
  return 0;
}
